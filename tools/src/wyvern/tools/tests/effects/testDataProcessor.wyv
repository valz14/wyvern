require stdout
import effects.network11
import effects.dataProcessor

val n11 = network11(stdout)

val dp11 = dataProcessor(n11, stdout) // should work
dp11.processData() // print "Data processor w/ effect abstraction: ", "data received", then "data processed"